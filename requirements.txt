# requirements.txt — Final version for Railway
# httpx pinned to compatible range for ollama-python 0.1.2

fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.9.0

# Solana
solana>=0.34.0
solders>=0.20.0

# HTTP clients
requests>=2.32.0
httpx>=0.26.0,<0.27.0          # ← CRITICAL: must stay below 0.27.0

# Ollama client (only real versions available)
ollama-python>=0.1.0,<0.2.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
structlog>=24.4.0

# Optional: external LLM fallback (uncomment when needed)
# openai>=1.0.0
