# Pattern Blue Swarm Railway Requirements
# Minimal dependencies optimized for Railway deployment

# Web framework
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.9.0

# Solana blockchain integration
solana>=0.34.0
solders>=0.20.0

# HTTP clients
requests>=2.32.0
httpx>=0.27.0

# Ollama integration (FIXED: use available version)
ollama-python>=0.1.0,<0.2.0

# Configuration and utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
structlog>=24.4.0

# Optional: External LLM fallback (comment out if not needed)
# openai>=1.0.0

# Note: Keep dependencies minimal for Railway memory constraints
# Avoid heavy ML libraries (torch, tensorflow, etc.)
